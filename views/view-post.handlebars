<div class="post">
    <div class="d-flex post-header">
        <h2>{{post.title}}</h2>
        <p>by {{post.user.username}} on {{format_date post.created_at}}</p>
    </div>
    <p class="post-content">{{post.text}}</p>
</div>

{{#each post.comments}}
<div class="comment">
    <div class="d-flex comment-header">
        <h2>{{user.username}} 
            <span>
                on {{format_date created_at}}
            </span> 
        </h2>
    </div>
    <p class="post-content">{{text}}</p>
</div>
{{/each}}